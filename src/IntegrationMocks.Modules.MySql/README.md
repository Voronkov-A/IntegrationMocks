# IntegrationMocks.Modules.MySql

## Brief
Library that contains infrastructure service classes for MySql binding.

* `DockerMySqlService` - starts MySQL in docker.
* `EnvironmentMySqlService` - exposes contract for already running MySQL instance.
