# IntegrationMocks.Modules.Postgres

## Brief
Library that contains infrastructure service classes for Postgres binding.

* `DockerPostgresService` - starts postgres in docker.
* `EnvironmentPostgresService` - exposes contract for already running postgres instance.
